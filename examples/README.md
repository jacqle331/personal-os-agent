# Example: A Filled-Out Setup

This folder shows what your files look like *after* you've replaced the
placeholders. **Do not edit these** — they're a reference. Your real files live
in the repo root (and are git-ignored).

Meet "Jordan," a busy parent running all three agents.

## Files
- [`CURRENT_CONTEXT.example.md`](./CURRENT_CONTEXT.example.md) — a live weekly snapshot
- [`SESSION_LOG.example.md`](./SESSION_LOG.example.md) — two sample check-in entries

Use these to see the *shape* of good entries, then make your own.
